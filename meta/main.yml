---
galaxy_info:
  role_name: postfix  # if absent directory name hosting role is used instead
  namespace: acromedia  # if absent, author is used instead
  author: Acro Media Inc.
  description: Install postfix so websites (and the server itself) can send out email
  company: Acro Media Inc.
  license: GPLv3
  min_ansible_version: 2.0
  platforms:
  - name: Ubuntu
    versions:
    - trusty
    - xenial
    - bionic
  - name: EL
    versions:
    - 6
    - 7
  galaxy_tags: []
dependencies: []
